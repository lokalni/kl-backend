#
# Template file to keep track of all env variables required to initialize django dockerized app.
#
#

# Required Env Vars
DJ_SECRET_KEY=ten_klucz_to_nie_jest_prawdziwy_wygeneruj_se
DJ_DATABASES_DEFAULT_URL=postgresql://kl-database/main?user=db-user&password=db-pass

# With defaults
DJ_BBB_DOMAIN_ALLOWED=.lokalni.pl

# Defaults overrides for local development
DJ_DEBUG=True
DJ_ALLOWED_HOSTS=localhost,127.0.0.1
DJ_CORS_ORIGIN_WHITELIST=http://localhost:8080,http://localhost

